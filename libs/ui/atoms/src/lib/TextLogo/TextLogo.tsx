import { Image } from '@inheartive/ui/atoms';
import { textLogo } from '@inheartive/ui/assets';

export const TextLogo = () => {
  return <Image source={textLogo} alt='Text logo' />;
};

export default TextLogo;
