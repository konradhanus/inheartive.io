import styled from 'styled-components/native';
import { Text } from '@inheartive-atoms';

const TextStyled = styled(Text)`
    color: #fff;
    max-height: 100%;
    position: absolute;
    width: 60px;
    text-align: center;
`;

export default TextStyled;
