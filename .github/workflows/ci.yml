name: CICDCDROM

on:
  pull_request: {}
  workflow_dispatch: {}
  push:
    branches:
      - main
      - release

jobs:
  konrad:
    name: My First Job
     steps:
      - name: Not on Monday or Wednesday
